apiVersion: v1
kind: Service
metadata:
  name: messaging-engine
  namespace: open-ai-ussd-kernel
  labels:
    app.kubernetes.io/name: messaging-engine
    app.kubernetes.io/component: rust-engine
spec:
  selector:
    app.kubernetes.io/name: messaging-engine
  ports:
    - port: 50055
      targetPort: 50055
      name: grpc
  type: ClusterIP

apiVersion: v1
kind: Service
metadata:
  name: python-gateway
  namespace: open-ai-ussd-kernel
  labels:
    app.kubernetes.io/name: python-gateway
    app.kubernetes.io/component: gateway
spec:
  selector:
    app.kubernetes.io/name: python-gateway
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  type: ClusterIP

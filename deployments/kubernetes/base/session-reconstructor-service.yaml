apiVersion: v1
kind: Service
metadata:
  name: session-reconstructor
  namespace: open-ai-ussd-kernel
  labels:
    app.kubernetes.io/name: session-reconstructor
    app.kubernetes.io/component: rust-engine
spec:
  selector:
    app.kubernetes.io/name: session-reconstructor
  ports:
    - port: 50051
      targetPort: 50051
      name: grpc
  type: ClusterIP

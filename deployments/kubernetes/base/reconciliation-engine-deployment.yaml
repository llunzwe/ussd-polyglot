apiVersion: apps/v1
kind: Deployment
metadata:
  name: reconciliation-engine
  namespace: open-ai-ussd-kernel
  labels:
    app.kubernetes.io/name: reconciliation-engine
    app.kubernetes.io/component: rust-engine
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: reconciliation-engine
  template:
    metadata:
      labels:
        app.kubernetes.io/name: reconciliation-engine
        app.kubernetes.io/component: rust-engine
    spec:
      containers:
        - name: reconciliation-engine
          image: openai-ussd-kernel/reconciliation-engine:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 50058
              name: grpc
          envFrom:
            - configMapRef:
                name: reconciliation-engine-config
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 512Mi
          livenessProbe:
            grpc:
              port: 50058
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            grpc:
              port: 50058
            initialDelaySeconds: 5
            periodSeconds: 10
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app.kubernetes.io/name
                      operator: In
                      values:
                        - reconciliation-engine
                topologyKey: kubernetes.io/hostname

apiVersion: v1
kind: Service
metadata:
  name: ledger-query-service
  namespace: open-ai-ussd-kernel
  labels:
    app.kubernetes.io/name: ledger-query-service
    app.kubernetes.io/component: rust-engine
spec:
  selector:
    app.kubernetes.io/name: ledger-query-service
  ports:
    - port: 50057
      targetPort: 50057
      name: grpc
  type: ClusterIP
